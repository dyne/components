// import { expect } from '@playwright/test';
import { test } from '@stencil/playwright';

test.describe('dyne-button', () => {
  test('should render the correct name', async () => {
    // The path here is the path to the www output relative to the dev server root directory
    // await page.goto('/components/dyne-code-editor/test/dyne-code-editor.e2e.html');
    // Rest of test
    // const component = await page.locator('dyne-code-editor');
    // await expect(component).toHaveText(`ciao`);
  });
});
